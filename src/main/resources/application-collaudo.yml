spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://qtmdev.quicare.com:3306/qtmdashboard_${APP_KEYCLOAK_REALM_CODE:realmCode}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:dago}
    driver-class-name: com.mysql.cj.jdbc.Driver
