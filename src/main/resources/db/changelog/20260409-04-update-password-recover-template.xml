<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="20260409-04-update-password-recover-template" author="copilot">
        <update tableName="mail_template">
            <column name="body" value="Ciao ${firstName},

Abbiamo ricevuto una richiesta di reset password per il tuo account.
Apri questo link per impostare una nuova password:

${resetUrl}

Se non hai richiesto tu il reset, ignora questa mail.

Cordiali saluti,
Il team QTM"/>
            <where>code = 'PASSWORD_RECOVER' AND language = 'it'</where>
        </update>
        <update tableName="mail_template">
            <column name="body" value="Hi ${firstName},

We received a password reset request for your account.
Open this link to set a new password:

${resetUrl}

If you did not request this, please ignore this email.

Best regards,
The QTM Team"/>
            <where>code = 'PASSWORD_RECOVER' AND language = 'en'</where>
        </update>
    </changeSet>

</databaseChangeLog>